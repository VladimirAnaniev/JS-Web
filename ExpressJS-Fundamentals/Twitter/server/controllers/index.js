const home = require('./home-controller')
const users = require('./users-controller')
const admins = require('./admins-controller')
const tweets = require('./tweets-controller')

module.exports = {
  home, users, admins, tweets
}
